# Use an official Node.js image to build the app
FROM node:18-alpine AS build

# Create a user to run the app as (instead of root)
RUN addgroup -S app && adduser -S app -G app

# Set the working directory in the container
WORKDIR /app

# Copy package.json and package-lock.json to the working directory
COPY package*.json /app/

# Install dependencies
RUN npm install

# Copy the rest of the application code to the working directory
COPY . /app/

# Expose the port Angular will use (default is 4200)
EXPOSE 4200

# Start the Angular development server
# using npx to ensure local CLI is used, and
# accessible to all network interfaces (e.g. other containers) inside the docker network
CMD ["npx", "ng", "serve", "--host", "0.0.0.0"]
